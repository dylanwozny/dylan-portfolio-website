# Functions

Returns of a function are captured in a variable.

    let square = function (x) {
    return x * x;
    };

    console.log(square(12));

    let calculation = square(3);

    console.log(calculation);


# Variable declarations

VAR is GLOBAL scope

LET and CONST are local scope
